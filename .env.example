# Example environment configuration for shopapi backend
# Copy this file to .env and fill in the actual values.

DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=your_db_name
# Set to true if your PostgreSQL instance requires SSL (e.g., managed cloud services)
DB_SSL=false
